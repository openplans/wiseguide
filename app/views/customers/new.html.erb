<h1>New customer</h1>

<%= form_for @customer, :html => { :multipart => true }  do |f| %>
  <%= render 'form', :f => f %>

  <div class="actions">
    <%= f.submit "Save" %>
  </div>
<% end %>

<%= link_to 'Back', customers_path %>
