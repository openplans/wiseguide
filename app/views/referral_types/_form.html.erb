<%= form_for(@referral_type) do |f| %>
  <% if @referral_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@referral_type.errors.count, "error") %> prohibited this referral_type from being saved:</h2>

      <ul>
      <% @referral_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.text_field :id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
